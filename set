 std::vector<int>v{1,3,8,45,75};
    std::set<int,greater<int >>s{10,7,54,85};
    set<int>::iterator i;
    for (i=s.begin();i!=s.end();i++)
    {
        cout<<*i;
    }
    cout<<endl;
    std::set<int,greater<int>>s1(v.begin(),v.end());
    for (i=s1.begin();i!=s1.end();i++)
    {
        cout<<*i;
    }