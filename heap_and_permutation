#include <map>
#include <set>
#include <list>
#include <cmath>
#include <ctime>
#include <deque>
#include <queue>
#include <stack>
#include <string>
#include <bitset>
#include <cstdio>
#include <limits>
#include <vector>
#include <climits>
#include <cstring>
#include <cstdlib>
#include <fstream>
#include <numeric>
#include <sstream>
#include <iostream>
#include <algorithm>
#include <unordered_map>
#include<string>
using namespace std;

int main(){

/*int a[]={1,2,3,4,5,5};
   int t= sizeof(a)/sizeof(int);
    std::make_heap(a,a+t);
    std::sort_heap(a,a+t,std::greater<int>());
    cout<<std::accumulate(a,a+t,1,multiplies<int>())<<endl;
    cout<<std::inner_product(a,a+t,a,0);//1+2*2+3*3.........*/
    vector<int > v;
    vector<int>::iterator i;
    for (int j = 0; j <6; ++j) {
        v.push_back(j+1);
    }
    int count=0;
    while(std::next_permutation(v.begin(),v.end()))
    {
        count++;
    }
    cout<<count+1;


    return 0;


}




